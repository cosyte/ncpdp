---
"@cosyte/ncpdp": patch
---

Correct the README status line: `@cosyte/ncpdp` is published.

The README status blockquote still claimed the package was "not yet published to npm." That is no
longer true — `@cosyte/ncpdp` is published on npm at `0.0.1` and public. The line now reflects the
published, public, still-pre-alpha (`0.0.x` ladder) reality. The capability prose is unchanged and
was already accurate. Documentation only — no runtime or API change.
