---
"@cosyte/ncpdp": patch
---

Documentation only: the README links to a separate dates and times reference instead of carrying
it inline.

The project guide does the same for its tech stack, required checks, standing disciplines and
project specifics. Every word is unchanged and nothing a consumer calls, imports or branches on has
moved.
